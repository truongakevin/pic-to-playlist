# Disabled deployment scripts

These scripts record the previous manual Mac-to-Linux deployment process. They
use obsolete paths, domains, and destructive replacement commands. Their
`.disabled` suffix is intentional. Production deployment is owned by GitHub
Actions and these files must not be executed.
