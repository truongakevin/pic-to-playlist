# Linux runtime snapshot

This directory preserves the manually edited Pic-to-Playlist Node source found
in the old Linux runtime during the August 2026 restructure. Those edits were
not present in the Git repository.

The snapshot is historical and is not deployed. Credentials, uploaded files,
and `node_modules` were intentionally excluded.
